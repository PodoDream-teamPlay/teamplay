<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="edu.spring.ex00.Pointmapper">
  
 <insert id="insert">
 insert into podo_point (userid, ptime) values (#{userid}, sysdate) 
 </insert>
 
 <select id="select" resultType="Date">
 select ptime from podo_point where pnum = (
    select max(pnum) from (
        select pnum from podo_point where userid = #{userid}
    )
); 
 </select>
  
  
  
  </mapper>